{/* Transfer to home.jsx
var React = require('react');

class Index extends React.Component {

  render(props){

        return (
            <React.Fragment>
                      <div>
                        <h1>Here are the list of recipes available</h1>
                            {this.props.displayAll}
                        </div>
             </React.Fragment>
        );
    }
}

module.exports = Index;

                            // {displayAll} */}
var React = require('react');
var DefaultLayout = require('./layouts/default');

class Add extends React.Component {
  render() {

    return (
            <DefaultLayout>

            </DefaultLayout>
    );
  }
}

module.exports = Add;
var React = require('react');
var DefaultLayout = require('./layouts/default');

class View extends React.Component {
  render() {

    return (
            <DefaultLayout>

            </DefaultLayout>
    );
  }
}

module.exports = View;